package br.com.akesofertas.cupons.domain;

/** Evidência usada para resolver o itemId de um card da listagem do cupom. */
public enum FonteDescobertaProdutoCupom {
    SSR_STRUCTURED,
    DOM_METADATA,
    HREF_WID,
    DIRECT_URL
}
